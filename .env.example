# WildScape Europe environment template
# Copy this file to .env for local overrides.

# Optional public Mapbox token for authenticated map rendering.
# The application still runs with deterministic local service data when omitted.
VITE_MAPBOX_ACCESS_TOKEN=
